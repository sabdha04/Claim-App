<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pengelolaan User</title>
    <link rel="stylesheet" href="/css/user.css">
</head>
<body>
    <h2>Pengelolaan User</h2>
    <ul>
        <% users.forEach(user => { %>
            <li>
                <div class="username"><strong>Username: <%= user.username %></strong></div>
                <div class="role"> Role : <%= user.role %></div>
                <div class="actions">
                    <a href="/api/auth/users/<%= user.id %>/edit">Edit</a>
                    <a href="/api/auth/users/<%= user.id %>/delete" onclick="return confirm('Are you sure?')">Delete</a>                    
                </div>
            </li>
        <% }); %>
    </ul>
    
    <p><a href="/login">Tambah User Baru</a></p>
    <p><a href="/home" class="back-button">Kembali ke Halaman Home</a></p>

</body>
</html>
